export type HeaderProps = {
  bgColor: string,
  display?: string,
  padding: string | number,
}
export type NavigationProps = {
  navDisplay?: string,
  borderBottom?: string,
  bgColor?: string,
  linkPadding: string,
  linkColor: string,
  linkFontSize: string
}